import pytest

"""
Tier-1 tests are intentionally disabled.
They must never run without explicit constitutional approval.
"""


@pytest.mark.skip(reason="Tier-1 permit-only tests require constitutional approval.")
def test_placeholder():
    pass
